# Ядро харнеса «ЖКХ-Агент» работает на стандартной библиотеке Python 3.9+.
# Ниже — ОПЦИОНАЛЬНЫЕ зависимости для расширенных функций.

# Извлечение текста из PDF-квитанций (classify_input.py):
pdfplumber>=0.10 ; python_version >= "3.9"
# либо облегчённая альтернатива:
# PyPDF2>=3.0

# OCR фотографий уведомлений (classify_input.py):
# требует системный пакет tesseract-ocr с русским языком (tesseract-ocr-rus)
pytesseract>=0.3.10
Pillow>=10.0

# monitor_tariffs.py использует только urllib из стандартной библиотеки —
# дополнительных пакетов не требует.
