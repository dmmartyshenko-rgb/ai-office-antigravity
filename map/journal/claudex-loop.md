# Журнал: claudex-loop

## 2026-09-21 — Домен зарегистрирован; связка установлена на Claude-стороне
Пользователь попросил найти, склонировать «Claudex Loop», поставить зависимости,
сделать конфиг связки Claude+Codex и скрипты запуска для парной разработки и
перекрёстного ревью. Из семейства одноимённых репозиториев взят канонический
`chaseai-yt/claudex-loop` (2375★, оригинал), commit `8cf5e2c`. Это набор skills,
не сервис.

Сделано: исходник завендорен в `tools/claudex-loop/`; скилы установлены в
`.claude/skills/`; написаны `scripts/claudex/{claudex.config.sh, claudex-pair.sh,
claudex-review.sh, claudex-doctor.sh, README.md}` и корневой `.gitignore`.
Проверено: venv + PyYAML → `validate.py` чист, апстрим-тесты 23/23 зелёные;
доктор `OK=7 WARN=1 FAIL=0`; preflight лаунчеров корректно падает при отсутствии
Codex до запуска сессии (квота не тратится).

Блокер (не мой, за пользователем): Codex CLI в окружении нет и залогинить его тут
нельзя. Codex ходит на `api.openai.com`/`auth.openai.com` — тот же egress-барьер,
что зафиксирован в journal/route-loop (CONNECT 403 в облаке). Реальные пути: локально
или через открытие сетевой политики окружения. Поэтому статус домена — пауза.

`PROOF_CMD` связки по умолчанию задан как `python3 scripts/check_map.py`, чтобы
любой боевой прогон опирался на целостность мегакарты как критерий готовности.
